# Development Scripts

> Rule: a script earns its place here only after the manual task has been done
> at least twice, except where consistency is load-bearing (module scaffolding).

| Script | Purpose |
|---|---|
| `new-module.ps1` | Scaffold `docs/modules/<name>/` from `docs/modules/_TEMPLATE/` |
