# Global Decision Log — سجل القرارات العام

> **Global** product decisions only — decisions that span more than one
> module. Decisions confined to a single module go **only** in that module's
> `decisions.md` (never both; link instead of duplicating). Routing rule: if
> reversing the decision would take more than a day, write an ADR instead
> (`docs/architecture/decisions/`). Decision text is recorded in Arabic
> (per ADR-0002).

| Date | Decision — القرار | Why — السبب | Decided by |
|---|---|---|---|
